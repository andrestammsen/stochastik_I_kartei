% NOCHMAL ALLES DURCHGEHEN WEGEN FEHLER UND LAYOUT
% \VSPACE*{... CM} => MACHT IMMERIMMERIMMER DEN VSPACE :):):):):)
% PROBLEM: kleine griechische Buchstaben nicht fett mit \bm-Befehl
% PROBLEM: z.B. 3.6.4: dicke Buchstaben sollen "normal" ohne Mathe-Modus sein, aber mit dem Mal-Zeichen passt das nicht so ganz von alleine, muss irgendwie mit \, oder so getäuscht werden, habe jetzt die "krummen" Mathe-Symbol-Buchstaben genommen :()
\documentclass[a7paper,11pt,print,grid=front]{kartei}
\usepackage[T1]{fontenc}
\usepackage{ngerman}
\usepackage[latin1]{inputenc}
\usepackage{amsfonts, amsmath, amssymb, mathtools}
\usepackage{bm}
\usepackage[mathscr]{euscript}
\usepackage{enumitem}
\usepackage[onehalfspacing]{setspace}
%\usepackage[singlespacing]{setspace}
%\usepackage[doublespacing]{setspace}

\begin{document}

\begin{karte}[Stochastik-I]{Algebra \\ $ \sigma $-Algebra \\ Dynkin-System}[2.1.2]
Ein System von Teilmengen/Ereignissen einer beliebigen, nicht-leeren Menge $ X $ heißt \textbf{Algebra} über $ X $, falls
\begin{enumerate}[label=(\roman*), noitemsep]
\item $ X \in \calA $
\item $ A \in \calA \Longrightarrow \comp{A} \in \calA $
\item $ A_1, ... , A_n \in \calA \Longrightarrow \cup_{i=1}^{n} A_i \in \calA $ (n $ \geq $ 1 beliebig)
\item $ A_1, A_2, ...  \in \calA \Longrightarrow \cup_{i=1}^{\infty} A_i \in \calA \ \longrightarrow $ \bm{$\sigma$}\textbf{-Algebra}
\item $ A_1, A_2, ... p.d.\Longrightarrow \cup_{i=1}^{\infty} A_i \in \calA \ \longrightarrow $ \textbf{Dynkin-System}
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{von $\eps$ erzeugte/ \\ kleinste \\ $\sigma$-Algebra}[2.1.4]
Seien $X$ bel n-leere Menge, $\eps \subset \calP(X)$ System von TM von $X$, dann ist die kleinste $\sigma$-Algebra bzw die von $\eps$ \textbf{erzeugte} \bm{$\sigma$}\textbf{-Algebra} über $X$:
\cmone
$$\sigma(\eps) = \bcapp{\calA: \ \sigma \t{-Algebra über } X \t{ mit } \eps \subset \calA}{}{} \calA$$
\end{karte}

\begin{karte}[Stochastik-I]{von $\eps$ erzeugtes/ \\ kleinster \\ Dynkin-System}[2.1.5]
Seien $X$ bel n-leere Menge, $\eps \subset \calP(X)$ System von TM von $X$, dann ist das kleinste Dynkin-System bzw das von $\eps$ \textbf{erzeugte Dynkin-System} über $X$:
\cmone
$$\sigma(\eps) = \bcapp{\calD: \ \t{Dynkin-System über } X \t{ mit } \eps \subset \calD}{}{} \calD$$

	
\end{karte}
\begin{karte}[Stochastik-I]{Metrik \\ und \\ metrischer \\ Raum}[2.1.6]
Metrischer Raum ist bel n-leere Menge $X$ versehen mit Metrik (Abstand) $d$, wobei $d$ Abbildung vom Typ \cmhalf \\
\centering{$d: X \times X \ni (x,y) \longrightarrow d(x,y) \in \calR_{\geq 0}$} \cmhalf \\
\raggedright{mit den Eigenschaften} \mmthree
\begin{enumerate}[label=(\roman*), noitemsep]
\item $d(x,y) = 0 \Leftrightarrow x = y$ \ (Definitheit)
\item $d(x,y) = d(y,x) \ \forall x,y \ni X$ \ (Symmetrie)
\item $d/x,z) \leq d(x,y) \, + \, d(y,z) \ \forall x,y,z \in X$ \ ($\Delta$-Ungl)
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{$\eps$-Kugel \\ offene Menge \\ Gesamtheit der offenen Mengen}[unter 2.1.6]
\raggedright{\bm{$\eps$}\textbf{-Kugel um x}, $x \ni X$ bel, $\eps > 0$: \mmthree \\}
\centering{$K(x,\eps) := {y \ni X : d(x,y) < \eps}$ \mmthree \\}
\raggedright{TM $G \subset X$ heißt \textbf{offen} im metr Raum $(X,d)$, wenn gilt: \vspace{0.3 cm} \\}
\centering$\forall x \ni G \ \exists \eps > 0, \t{ sd } K(x,\eps) \subset G$ \mmthree \\
\raggedright{Gesamtheit aller off Mengen in $(X,d)$ bezeichnet mit \mmthree \\}
\centering{$\calG(X) := {G\subset X : G \ \t{ist offen}}$}
\end{karte}

\begin{karte}[Stochastik-I]{Borel-Mengen}[2.1.7]
\raggedright{Sei $(X,d)$ metr Raum, $\calG(X)$ Syst off Mengen in $X$, dann definiert man die $\sigma$-Algebra der \textbf{Borel-Mengen} als \cmhalf \\}
\centering{$\calB(X) := \sigma(\calG(X))$ \cmhalf \\}
\raggedright{Ist $(X,d) := \R$ mit üblicher Metrik, schreibt man \cmhalf \\}
\centering{$\calB(\R) = \calB^*$}
\end{karte}

\begin{karte}[Stochastik-I]{Halbstrahlen \\ als Erzeuger \\ für Borel-Mengen}[2.1.8]
Die Systeme der Halbstrahlen schreiben wir als \mmthree \\
$\calS_{r,a} = {]-\infty, x] : x \in \R}$ \\
$\calS_{r,o} = {]-\infty, x[ : x \in \R}$ \\
$\calS_{l,a} = {[-\infty, x[ : x \in \R}$ \\
$\calS_{l,o} = {]-\infty, x[ : x \in \R}$ \mmthree \\
Es gilt \mmthree \\
$\calB^* = \sigma(\calS_{r,a}) = \sigma(\calS_{r,a}) = \sigma(\calS_{r,a}) = \sigma(\calS_{r,a})$
\end{karte}

\begin{karte}[Stochastik-I]{additiv \\ und \\ $sigma$-additiv}[zu 2.2.1]
\raggedright {
Sei $(X, \calA)$ mb Raum. Er ist \textbf{additiv}, wenn }
$$
A_1, A_2, ... , A_n \in \calA \ pd \Longrightarrow \mu \left ( \bcupp{i=1}{n}{A_i} \right) = \summ{i=1}{n}{\mu(A_i)}
$$
und \bm{$\sigma$}\textbf{-additiv}, wenn
$$
A_1, A_2, ... \in \calA \ pd \Longrightarrow \mu \left ( \bcupp{i=1}{\infty}{A_i} \right)= \summ{i=1}{\infty}{\mu(A_i)}
$$
\end{karte}

\begin{karte}[Stochastik-I]{$\sigma$-subadditiv}[zu 2.2.1]
\raggedright {
Sei $(X, \calA)$ mb Raum. Er ist \bm{$\sigma$}\textbf{-subadditiv}, wenn }\cmone
$$
A_1, A_2, ... \in \calA \Longrightarrow \mu \left ( \bcupp{i=1}{\infty}{A_i} \right) \leq \summ{i=1}{\infty}{\mu(A_i)}
$$
\end{karte}

\begin{karte}[Stochastik-I]{nicht-negative \\ Mengenfunktion \\ (finiter) Maßraum \\ Wahrscheinlichkeitsraum}[2.2.1]
Seien $X$ n-leere Menge, $\emptyset \in \calA \subset \calP(X)$, dann ist $(X, \calA, \mu)$ eine n-neg Mengenfunktion auf $(X, \calA)$, wenn $\mu(\emptyset) = 0$ und: \newline
$\mu: \calA \ni A \longrightarrow \mu(A) \in [0,\infty]$
\begin{enumerate}[label=(\roman*), nosep]
\item
$\mu$ additiv $ \longrightarrow (X, \calA, \mu)$ ist \textbf{Inhalt}
\item
$\mu \ \sigma$-additiv $ \longrightarrow \mu$ ist \textbf{Maß}, $(X, \calA, \mu)$ ist \textbf{Maßraum}
\item
$\mu(X) < \infty \longrightarrow (X, \calA, \mu)$ ist \textbf{finiter Maßraum} \\
$\mu(X) = 1 \longrightarrow  (X, \calA, \mu)$ ist  \textbf{Wahrsch-Raum}
\item
$\exists \, A_1, A_2, ... \in \calA, \mu(A_i) < \infty, \bcupp{i=1}{\infty}{A_i} = X \longrightarrow (X, \calA, \mu) $ ist \bm{$\sigma$}\textbf{-finiter Maßraum}
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Rechenregeln \\ Maßraum}[2.2.2]
Sei $(X, \calA, \mu)$ Maßraum((i) - iii)), und finit (iv) - vi)) dg: \newline
\begin{enumerate}[label=(\roman*), nosep]
\item
$\mu$ ist additiv
\item
$\mu$ ist $\sigma$-subadditiv
\item
$\mu$ ist monoton, dh $\mu(A) \leq \mu(B)$
\item
$\mu(B \setminus A) = \mu(B) - \mu(A)\  \forall A \subset B \in \calA$
\item
$\mu(A_1 \cup A_2) = \mu(A_1) + \mu(A_2) - (A_1 \cup A_2) \ \forall A_1, A_2 \in \calA$
\item
Allgemeines Additionsgesetz / Ein-Ausschluss-Formel
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Konvergenz \\ Maßraum \\ von unten und \\ von oben}[2.2.3]
Sei $(X, \calA, \mu)$ Maßraum, $A_1, A_2, ... \in \calA$, dg (v. unten/oben):
\begin{enumerate}[label=(\roman*), nosep]
\item
Falls $A_n \subset A_{n+1} \ \forall n \in \N$, dh $(A_n)_{n \geq 1}$ mon wachs, dg: \newline
$$
\lim_{n\to\infty} \mu(A_n) = \mu(A), \ A = \bcupp{n=1}{\infty}{A_n}
$$
\item
Falls $A_n \supset A_{n+1} \ \forall n \in \N$, dh $(A_n)_{n \geq 1}$ ist mon fall, $\exists n_0 \in \N \t{ sd } \mu(A_{n_0}) < \infty$, dg:  \newline
$$
\lim_{n\to\infty} \mu(A_n) = \mu(A), \ A = \bcapp{n=1}{\infty}{A_n}
$$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Maßeindeutigkeit}[2.2.4]
Sei $X \neq \emptyset, \ \eps \subset \calP(X) \ \cap$-stabiler Erz von $\calA = \sigma(\eps)$, der eine wachs Folge $ (E_n)_{n \in \N} $ enhält mit $ E_n \uparrow X $, und $ \mu_1, \ \mu_2 $ Maße auf $ (X, \calA) $ mit den Eigenschaften \newline
\begin{enumerate}[label=(\roman*)]
\item
$\mu_1(E) = \mu_2(E) \ \forall E \in \eps$
\item
$\mu_1(E_n) < \infty, \ \forall n \in \N$
\end{enumerate}
\cmhalf
dg: $\mu_1(A) = \mu_2(A) \ \forall A \in \calA$, dh die Maße sind identisch
\end{karte}

\begin{karte}[Stochastik-I]{Maßerweiterungssatz \\ von \\ Carathéodory}[2.2.5]
Sei $X$ eine bel Menge, $\calA_0 \subset \calP(X)$ eine Alg auf $X$ und $\mu: \calA_0 \longrightarrow [0,\infty]$ ein Inhalt mit $\sigma$-Additivität, also \newline
$ \forall (A_n)_{n \in \N} \subset \calA_0 \t{ pd mit } \cup_{n=1}^{\infty} A_n \in \calA_0:$
$$
\mu \left( \bcupp{n=1}{\infty}{A_n} \right ) = \summ{n=1}{\infty}{\mu(A_n)}
$$
dann $ \exists \t{ Maß } (X, \sigma(\calA_0), \hat{\mu} \t{ mit }  \hat{\mu(A)} = \mu(A) \ \forall A \in \calA_0, \newline
\t{dh } \hat{\mu} \t{ ist Forts von Inhalt } (X, \calA_0, \mu) \t{ zu Maß } (X, \sigma(\calA_0))
$
\end{karte}

\begin{karte}[Stochastik-I]{$\mu$-fast sicher \\ $\mu$-Nullmenge}[2.2.7]
Seien $(X, \calA, \mu)$ Maßraum, $AUS(x)$ Aussage über $x \in X$, dann heißt $AUS$ gilt \bm{$\mu$}\textbf{-fast sicher} bzw. $AUS \, \mu-fs$, wenn:
$$
\quad \mu \left( \{x \in X: AUS(x) = FALSE \} \right) = 0
$$
$A \in \calA \t{ mit } \mu(A) = 0$ bezeichnet man als \bm{$\mu$}\textbf{-Nullmengen}. Insgesamt gilt also:
$$
AUS \ \mu \t{-fs} \Leftrightarrow \{x \in X: AUS(x) = FALSE\} \  \mu \t{-Nullmenge}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Messbarkeit}[2.3.1]
Seien $(X, \calA)$ und $(Y, \calB)$ mb Räume. Eine Abbildung \newline
$$
f: X \ni x \longrightarrow f(x) \in Y
$$
ist \bm{$\calA - \calB$}\textbf{ messbar} gdw \newline
$$
\forall B \in \calB: f^{-1}(b) \in \calA \t{ bzw } f^{-1} \subset \calA
$$
\end{karte}

\begin{karte}[Stochastik-I]{Messbarkeit \\ erleichtert}[2.3.2]
Sei $f: (X, \calA) \longrightarrow (Y, \calB)$ eine Funktion, $\eps \subset \PP(Y)$ ein Erz-System von $\calB$, dh $\sigma(\eps) = \calB$, dann sind äquivalent \newline
\begin{enumerate}[label=(\roman*)]
\item $f^{-1}(\eps) \subset \calA$
\item $f$ ist $\calA-\calB$-messbar
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{numerische \\ Abbildungen}[2.3.3]
Eine \textbf{numerische Abbildung} der Form \newline
$$
f: (X, \calA) \longrightarrow (\overline{\R}, \calB(\overline{\R}))
$$
heißt \textbf{$\calA$ mb}, falls $\calA-\calB(\overline{\R})$ mb oder $\calA-\calB^*$ mb. \newline
Die Mengen der \bm{$\calA$}\textbf{ mb Abbildungen} schreibt man
$$
\overline{Z(X, \calA)} := \{f: (X, \calA) \longrightarrow (\overline{\R}, \calB(\overline{\R})) \ | \ f \t{ ist } \ \calA-\calB^*(\overline{\R}) \ mb \}
$$
\textit{n-neg: $\Z_+$; $\overline{Z}$ bedeutet $Z$ incl $\infty$}
\end{karte}

\begin{karte}[Stochastik-I]{Halbstrahlen \\ als \\ Erzeuger}[2.3.4]
Für eine Abb $F: (X, \calA) \longrightarrow \overline{\R}$ sind folgende Auss \newline äquivalent:
\begin{enumerate}[label=(\roman*), noitemsep]
\item $f \in \overline{Z(X, \calA)}$
\item $\{f \leq r\} \in \calA \ \forall r \in \R$
\item $\{f < r\} \in \calA \ \forall r \in \R$
\item $\{f \geq r\} \in \calA \ \forall r \in \R$
\item $\{f > r\} \in \calA \ \forall r \in \R$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Indikatorfunktion}[2.3.5]
Die \textbf{Indikatorfunktion} ist auf mb Raum $(X, \calA)$ \newline 
mit $A \subset X$ definiert als
$$
I_A: (X,\calA) \ni x \longrightarrow = I_A(x) \equiv I(x \in A) = \twopartdef{1}{x \in A}{0}{x \in \comp{A}}
$$
wobei $I_A \in (R, \calB^*)$, und es sind äquivalent
\begin{enumerate}[label=(\roman*)]
\item $I_A$ ist $\calA$-mb
\item $A \in \calA$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Komposition/ \\ Hintereinanderschaltung \\ messbarer \\ Funktionen}[2.3.6]
Geg seien die mb Funktionen
$$
f: (X, \calA) \longrightarrow (Y, \calB), \ g: (Y, \calB) \longrightarrow (Z, \calC)
$$
Die Komposition/Hintereinanderschaltung ist auch wieder mb:
$$
h = g \circ f \t{ ist }\calA-\calC \t{ mb}
$$
\textit{f ist innere Funktion / wird zuerst ausgeführt}
\end{karte}

\begin{karte}[Stochastik-I]{Messbarkeit \\ endlicher \\ Summen und \\ Produkte}[2.3.6]
Sei der gemeinsame Raum $(X, \calA)$ mb, dg für Funktionen über diesem Raum: \cmhalf \\
Endliche Summen und Produkte numerischer Funktionen sind wieder Borel-mb (falls definiert). \cmhalf \\
\cmhalf
Mit $f \in \overline{Z(X, \calA)}$ gilt: \\
$1/f \in \overline{Z(X, \calA)}$ (falls definiert).
\end{karte}

\begin{karte}[Stochastik-I]{Suprema / Infima \\ Limites superior/inferior \\ $\calA$-messbarer \\ Abbildungen}[2.3.8]
Sei $(f_n)_{n \in \N}$ bel Folge von Fnkt aus $\overline{Z(X, \calA)}$, dann sind ebenfalls wieder aus $\overline{Z(X, \calA)}$:
\vspace*{0.3 cm}
\begin{enumerate}[label=(\roman*), nosep]
\item inf $f_n$; $\forall x \in X:$ inf $f_n(x) := $ inf $\{f_n(x): n \in \N\}$
\item sup $f_n$; $\forall x \in X:$ usp $f_n(x) := $ sup $\{f_n(x): n \in \N\}$
\item lim inf $f_n$; $\forall x \in X:$ lim inf $f_n(x) \\ := $ lim inf $\{f_n(x): n \in \N\}$
\item lim sup $f_n$; $\forall x \in X:$ lim sup $f_n(x) \\ := $ lim sup $\{f_n(x): n \in \N\}$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Bildmaß}[2.3.9]
Sei $f: (X, \calA, \mu) \longrightarrow (Y, \calB)$ eine $\calA - \calB$ mb Abb, \newline
dann wird durch $f$ und das Maß $\mu$ der mb Raum $(Y, \calB)$ auch zu einem Maßraum $(Y, \calB, \mu_f)$ mittels
$$
\mu_f: \calB \ni B \longrightarrow \mu_f(B) := \mu(f^{-1}(B)) \in [0,\infty]
$$
$\mu_f$ ist das \textbf{Bildmaß} von $\mu$ unter $f$.
\end{karte}

\begin{karte}[Stochastik-I]{Maßintegral \\ Schreibweisen}[unter 2.4]
Folgende Schreibweisen sind für das Maßintegral gebräuchlich:
$$
\mu(f) \equiv \int_{}^{}f \, \t{d}\mu \equiv \int_{}^{} f(x)\mu(\t{d}x) \equiv \int_{X}{} f \, \t{d}\mu \equiv \int_{y}{} f(x) \mu(\t{d}x) 
$$
Bezogen auf eine Menge $A \in \calA$ schreibt man:
$$
\int_{A}{} f(x)\mu(\t{d}x) \equiv \int_{A}{} f \, \t{d}\mu \equiv \mu(f;A) \equiv \mu(f \cdot I_A) \equiv \int_{X}{} f \, I_A \, \t{d}\mu
$$
\end{karte}

\begin{karte}[Stochastik-I]{Maßintegral \\ der \\ Indikatorfunktion}[unter 2.4.1]
Mit $f = I_A, \ A \in \calA$ ist dann $f \in Z_+(X,\calA)$, und man definiert das \textbf{Maßintegral der Indikatorfunktion $\bm{I_A}$ } durch \cmonehalf \\
$$
\mu(I_A) = \int_{}{} I_A \ \t{d}\mu := \mu(A)
$$
\end{karte}

\begin{karte}[Stochastik-I]{nicht-negative \\ Elementarfunktionen}[2.4.1]
Die Menge der \textbf{nicht-negativen Elemtarfunktionen} zum mb Raum $(X, \calA)$ wird definiert durch \cmonehalf \\
$$
E_+(X, \calA) := \{f \in Z_+(X, \calA): f(X) \ \t{ist eine endl Menge}\}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Summenschreibweise \\ Elementarfunktion}[2.4.2]
Sei $f \in E_+(X, \calA)$, dann gibt es $A_1, A_2, ... , A_n \in \calA \t{ und } \\ \alpha_1, \alpha_2, ... , \alpha_n \in R_\geq$ mit \cmonehalf \\
$$
f(x) = \summ{i=1}{n}{\alpha_i \, I_{A_i}(x)}, \ \forall x \in X
$$
\end{karte}

\begin{karte}[Stochastik-I]{Maßintegral \\ nicht-negative \ Elementarfunktion}[2.4.3]
Sei $f \in E_+(X, \calA)$ mit der Darstellung
$$
f(x) = \summ{i=1}{n}{\alpha_i \, I_{A_i}(x)}, \ \forall x \in X
$$
dann wird das \textbf{Maßintegral einer nicht-negativen \\ Elementarfunktion} definiert durch
$$
\mu(f) \equiv \mu \left( \summ{i=1}{n}{\alpha_i \, I_{A_i}} \right) \equiv \int_{}^{} f \, \dd\mu := \summ{i=1}{n}{\alpha_i \, \mu(A_I)}
$$
\textit{(Dabei ist die Wahl der Summendarstellung irrelevant}
\end{karte}

\begin{karte}[Stochastik-I]{Approximationssatz}[2.4.5]
\vspace*{1.5 cm}
%\cmonehalf . \\ \cmhalf . \\
% WIE MACHE ICH DEN ABSTAND DIREKT AM ANFANG DER FLASHCARD?
Sei $f \in \overline{Z_+(X, \calA)}$, dann gibt es eine mon wachs Folge $(f_n)_{n \in \N} \subset E_+(X, \calA) \t{ mit } lim_{n \to \infty} f_n(x) = f(x) \ \forall x \in X$. Dabei ist die Konvergenz goeichm auf $X$, falls $f$ beschr ist.
\end{karte}

\begin{karte}[Stochastik-I]{Maßintegral \\ nicht-negativer \\ messbarer \\ Funktionen}[2.4.6]
Sei $f \in \overline{Z_+(X, \calA)} \t{und} (f_n)_{n \in \N} \subset E_+(X, \calA)$ eine gemäß Approximationssatz zu $f$ gewählte Folge n-neg Elementarfunktionen, dann wird das \textbf{Maßintegral einer nicht-negativen messbaren Funktion} definiert durch
$$
\mu(f) \equiv \int_{}^{} f\, \dd\mu := \sup_{n \in \N} \int_{}^{} f_n \, \dd\mu \equiv \sup_{n \in \N} \mu(f_n)
$$
\end{karte}

\begin{karte}[Stochastik-I]{Positivteil \& Negativteil \\ von \\ Funktionen}[2.4.8]
Sei $f \in \overline{Z(X,\calA}$ bel, dann werden der \textbf{Positivteil} \newline
und \textbf{Negativteil} von $f$ als zwei neue Funktionen aus \newline
$\overline{Z_+(X,calA)}$ definiert durch \cmhalf \\
$$
f^+(x) := max\{f(x),0\}, \ f^-(x) := max\{-f(x),0\}, \ x \in X
$$
\end{karte}

\begin{karte}[Stochastik-I]{Maßintegral \\ von $f$ bzgl $\mu$ \\ oder \\ $\mu$-Integral von f}[2.4.9]
$f \in \overline{Z(X,\calA)}$ bel, $f^+ / f^-$ Positiv-/Negativteil von $f$:
\begin{enumerate}[label=(\roman*), noitemsep] 
\item Mind $\mu(f^+) \t{ oder } \mu(f^-)$ endl, dann ist das 
\textbf{Maßintegral von $\bm{f}$ bzgl $\bm{\mu}$} bzw \textbf{$\bm{\mu}$-Integral von $\bm{f}$}:
$$
\mu(f) \equiv \int_{}^{}f \dd\mu := \int_{}^{} f^+ \dd\mu - \int_{}^{} f^- \dd\mu \equiv \mu(f^+) - \mu(f^-)
$$
\item Integral exist: $f$ heißt \textbf{$\bm{\mu}$-quasiintegrierbar}
\item Integral exist und endl: $f$ ist \textbf{$\mu$-integrierbar}
\item $\mu(f^+) = \infty \t{ und } \mu(f^-) = \infty \t{: } \mu(f)$ nicht definiert
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Gesamtheit \\ $\mu$-integrierbarer \\ Funktionen}[2.4.10]
$f \in \overline{Z(X,\calA)} $ ist $\mu$-integrierbar gdw $|f|$ ist $\mu$-integrierbar. Die Gesamtheit aller $\mu$-integrierbaren Funktionen wird daher im Folgenden bezeichnet mit
$$
\overline{L^1(X,\calA,\mu)} := \{ f \in \overline{Z(X,\calA)}: \mu(|f|) < \infty \}
$$
bzw
$$
L^1(X,\calA,\mu) := \{ f \in Z(X,\calA) : \mu(|f|) < \infty \}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Eigenschaften des Maßintegrals}[2.4.4]
$ f,g \in \overline{Z\XcalA} $ und $ \XcalAmu $ Maßraum, dg:
\begin{enumerate}[label=(\roman*)]
\item $ f = 0 \ \mae \Longrightarrow \intt{}{}{f}{\mu} = 0 $
\item $ \intt{}{}{f}{\mu} = 0 \Longleftrightarrow f = 0 \ \mae $
(falls $ f \geq 0 $)
\item $ \intt{}{}{g}{\mu} $ existiert und $ \intt{}{}{g}{\mu} = \intt{}{}{f}{\mu} \ \Longleftarrow $
\vspace{0.15 cm}
\newline
(falls $ \intt{}{}{f}{\mu} $ existiert und $ f = g \ \mae $)
\item $ f \ \mintable \ \Longrightarrow f $ ist endlich $ \mae $
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Algebraische Induktion}[2.4.15]
$ f,g \in \overline{Z\XcalA} $ und $ \XcalAmu $ Maßraum, dg:
\begin{enumerate}[label=(\roman*)]
\item Zeige $ AUS(f) $, wobei $ f = I_A $, $ \forall A \in \calA $
\item Zeige $ AUS(f) $, wobei $ f = \alpha I_A $, $ \forall A \in \calA $
\item Zeige $ AUS(f) \ \forall f \in \EplusXcalA $
\item Zeige $ AUS(f) \ \forall f \in \ZplusXcalAinf $
\item Zeige $ AUS(f) \ \forall f \in \ZXcalAinf $
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Satz von der monotonen Konvergenz}[2.4.17]
Sei $ \XcalAmu $ Maßraum, $ (f_n)_{n\in\R} $ eine $ \mae $ mon wachs
Folge n-neg Funktionen aus $ \ZXcalAinf  $ und $ f $ eine Funktion,
die $ \mae \ f(x) = lim_{n\to\infty} $, $ f_n (x) $ erfüllt, dg:
\cmone
$$
\intt{}{}{\lim_{n\to\infty} f_n}{\mu} = \intt{}{}{f}{\mu} = \lim_{n\to\infty} \intt{}{}{f_n}{\mu}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Satz von B. Levi}[2.4.18]
Sei $ \XcalAmu $ Maßraum, $ (f_n)_{n\in\N} $ Folge n-neg Funktionen
aus $ \ZXcalAinf $, dg:
\cmone
$$
\intt{}{}{\summ{n=1}{\infty}{} \, f_n}{\mu} = \summ{n=1}{\infty}{\intt{}{}{f_n}{\mu}}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Lemma von Fatou}[2.4.19]
Sei $ \XcalAmu $ Maßraum und $ (f_n)_{n\in\N} $ eine Folge n-neg \\
Funktionen aus $ \ZXcalAinf $, dg:
\cmone
$$
\intt{}{}{\liminf_{n\to\infty} f_n }{\mu} \ \leq \ \liminf_{n\to\infty} \intt{}{}{f_n}{\mu}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Lebesgues Satz von der majorisierten Konvergenz}[2.4.20]
Sei $ \XcalAmu $ Maßraum, $ (fn)_{n\in\N} $ Folge aus $ \ZXcalAinf $, die \\ punktweise gegen eine Funktion $ f $ konvergieren, $ g $ eine
n-neg $ \mintable $e Funktion mit der Eigenschaft, dass
$ |f_n| \leq g $, $ \mae $, dann folgt, dass $ f $ und $ f_n \ \mintable $ sind $\forall n \in \N $ und
\cmone
$$
\intt{}{}{f}{\mu} = \intt{}{}{\lim_{n\to\infty} f_n}{\mu} = \lim_{n\to\infty} \intt{}{}{f_n}{\mu}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Transformationssatz für Maßintegrale}[2.4.21]
Sei $ T:\XcalAmu \longrightarrow \YcalB $ eine $ \calA - \calB $ mb Abbildung und $ (Y,\calB, \mu_{T}) $ das induzierte Bildmaß. Ferner sei $ f \in \ZYcalB $ eine numerische Abbildung. Dann ist $ f \circ T \in \ZXcalAinf $, dg $ \forall B \in \calB $ (falls mind. ein Integral existiert):
\cmhalf
$$
\intt{T^{-1}(B)}{}{f \circ T}{\mu} = \intt{B}{}{f}{\mu_T}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Standardnormalverteilung}[unter 2.4.8]
\cmone
$$
\xi \sim \calN (1,0)
$$
\vspace{0.5 cm}
$$
\intt{A}{}{\frac{1}{\sqrt{2\pi}}\exp(\frac{-x^{2}}{2})}{x}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Radon-Nikodym Dichte}[2.4.22]
Sei $ \XcalAmu $ Maßraum und $ f \in \ZplusXcalAinf $. Dann wird wie unten ein neues Maß $ \XcalAnu $ definiert. Die Funktion $ f $ heißt \textbf{$ \mu $-Dichte von $ \nu $} oder \textbf{Radon-Nikodym Dichte} von $ \nu $ bzgl $ \mu $:
\cmhalf
$$
\nu: \ \calA \ni A \longrightarrow \nu(A) := \intt{A}{}{f}{\mu} \equiv \int{}{}{f \, I_A}{\mu}
$$
\newline
Schreibweise: $ d \nu = f d \mu $ (bzw. $ \nu = f \, \mu $, $ f = \frac{d\nu}{d\mu}) $
\end{karte}

\begin{karte}[Stochastik-I]{Name gesucht :)}[2.4.23]
Sei $ \XcalAmu $ Maßraum, $ f \in \ZplusXcalAinf $, $ d\nu := f \, d\mu $, $ \xi \in \ZXcalAinf $ eine mb Funktion, dg:
\vspace{0.5 cm}
$$
\xi \in \LoneXcalAnu \Longleftrightarrow \xi \cdot f \in \LoneXcalAmu
$$
\end{karte}

\begin{karte}[Stochastik-I]{Satz von Radon-Nikodym}[2.4.24]
Seien $ \mu $, $ \nu $ Maße auf dem mb Raum $ \XcalA$, $ \mu \ \sigma $-finit, dann sind äquivalent:
\cmhalf
\begin{enumerate}[label=(\roman*)]
\item $ \nu $ besitzt eine Dichte bzgl $ \mu $
\item $ \forall A \in \calA$: $ \mu(A) = 0 \Longrightarrow \nu(A) = 0 $
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{absolut-stetig \\ \& \\ singulär}[2.4.25]
$ \XcalA $ mb Raum, Maße $\mu $ und $ \nu $:
\cmhalf
\begin{enumerate}[label=(\roman*)]
\item $ \nu $ besitzt eine Dichte bzgl $ \mu $
\item $ \forall A \in \calA $: $ \mu(A) = 0 \Longrightarrow \nu(A) = 0 $
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Zerlegungssatz von Lebesgue}[2.4.25]
$ \XcalA $ mb Raum, Maße $ \mu $ und $ \nu $:
\cmone
\begin{itemize}
\item $ \nu $ lässt sich auf genau eine Weise als $ \nu = \nu_1 + \nu_2 $ darstellen
\item $ \nu_1 $ und $ \nu_2 $ sind Maße auf $ \XcalA $
\item $ \nu_1 \ll \mu $
\item $ \nu_2 \perp \mu $
\end{itemize}
\end{karte}

\begin{karte}[Stochastik-I]{kartesisches Produkt}[unter 2.5]
$ (X_i, \calA_i)_{1 \leq i \leq n} $ mb Räume:
\cmone
$$
X := \prodd{i=1}{n}{X_i} \equiv \btimess{i=1}{n}{X_i}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Projektionsabbildung}[unter 2.5]
\textit{
Alle Tupel werden auf ihre jeweils i-te Koordinate abgebildet, ähnlich einer orthogonalen Projektion auf eine Achse (aber formal anders). Für dieses i werden alle $ x_i $ aus $ X_i $ angenommen.
}
\cmone
$$
p_i: X \ni (x_q, x_2, ..., x_n) \longrightarrow p_i((x_1, ... x_n)) := x_i \in X_i
$$
\end{karte}

\begin{karte}[Stochastik-I]{Produkt-$\sigma$-Algebra}[2.5.1]
Die kleinste $\sigma$-Algebra, bzgl der alle $ n $ Projektionen mb sind, auch genannt das Produkt der $\sigma$-Algebren $ \calA_1, ..., \calA_n $:
\cmhalf
$$
\botimess{i=1}{n}{\calA_i} := \calA_1 \times ... \times \calA_n := \sigma \left( \bcupp{i=1}{n}{p_i^{-1}} (\calA_i) \right) \equiv \sigma(p_i, ..., p_n)
$$	
\end{karte}

\begin{karte}[Stochastik-I]{DAS Produktmaß}[2.5.3]
Das eind Produktmaß $ \pi $ der Maße $ \mu_1, \mu_2, ... , \mu_n $ auf \\
$ \calA = \botimess{i=1}{n}{\calA_i} $ mit $ A_i \in \calA_I $ für
$ 1 \leq i \leq n $ hat für $ n $ $ \sigma $-finite \\
Maßräume $ (X_i, \calA_i, \mu_i)_{1 \leq i \leq n} $ die folgende Eigenschaft und \\
mit $ \pi \equiv \botimess{i=1}{n}{\mu_i} $ ergibt sich ein neuer Maßraum:
$$
\pi (A_1 \times A_2 \times ... \times A_n) := \mu_1(A_1) \cdot \mu_2(A_2) \cdot ... \cdot \mu_n(A_n)
$$
$$
\left( \btimess{i=1}{n}{X_i}, \, \botimess{i=1}{n}{\calA_i}, \, \botimess{i=1}{n}{\mu_i} \right)
$$
\end{karte}

\begin{karte}[Stochastik-I]{Satz von Tonelli}[2.5.4]
$ (X_i, \calA_i, \mu_i) $ 2 $ \sigma $-finite Maßräume, $ (X, \calA, \pi) $ Produktraum, eind best Produkt-Maß $ \pi $, $ f \in \ZplusXcalAinf $ n-neg mb Funk:
\begin{align*}
(X_1, \calA_1) \ni x_1 \longrightarrow \inttt{}{}{f(x_1,x_2)}{\mu_2}{x_2} \in (\overline\R_\geq, \calB(\overline{\R}_\geq)\\
\intt{}{}{f}{\pi} = \int{}{}{\left( \inttt{}{}{f(x_1,x_2)}{\mu_2}{x_2} \right) }{\mu_1(}{x_1}) \\
= \int{}{}{\left( \inttt{}{}{f(x_1,x_2)}{\mu_1}{x_1} \right) }{\mu_2(}{x_2})
\end{align*}
\end{karte}

\begin{karte}[Stochastik-I]{Satz von Fubini}[2.5.5]
$ (X_i, \calA_i, \mu_i) $ 2 $ \sigma $-finite Maßräume, $ (X, \calA, \pi) $ der zug Produktraum mit dem eind best Produktmaß $ \pi $, $ f \in \ZXcalAinf $ eine $ \pi $-integrierbare Funktion, dann gilt ebenfalls Tonelli. 
\vspace{10 px} \\
\textit
{Zusätzlich wird also Integrierbarkeit bzgl des Produktmaßes gefordert. Dafür kann man bel, nicht nur n-neg Funktionen integrieren. Bei Tonelli reicht die Integrierbarkeit der iterierten Integrale aus. Bei Rückführung mehr- auf eindimensionale Integrale ist die Reihenfolge bei beiden egal. 
}
\end{karte}

\begin{karte}[Stochastik-I]{Experiment/ \\ Zufallsvektor/  \\ Zufallsvariable/ \\ Verteilung}[unter 3.1]
\begin{enumerate}[label=(\roman*), noitemsep]
\item 
Im W-Raum $ (\Omega, \calA, \PP) $ ist ein \textbf{Experiment} oder ein $ k $-dim \textbf{Zufallsvektor} $ \xi = (\xi_1, \xi_2, ... , \xi_k) $ eine $ \calA - \calB_k^* $ mb Abb, im Spezialfall $ k = 1 $ eine 
\vspace{0.3 cm} 
\textbf{Zufallsvariable}:
$ \xi: \Omega, \calA, \PP) \ni \omega \longrightarrow \xi(\omega) \in (\R_k, \calB_k^*) $
\vspace{0.3 cm} 
\item Ist $ \xi $ ein $ k $-dim ZV, dann wird das Bildmaß $ (\R^k, \calB_k^*, \PP_\xi) $ (die \textbf{Verteilung} von $ \xi $ unter $ \PP $) definiert
$$
\PP_\xi: \calB_k^* \ni \calB \longrightarrow \PP_\xi(B) := \PP(\xi^{-1}(B))
$$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Verteilungsfunktion}[3.1.2]
Sei $ \xi $ ein $ k $-dimensionaler ZV auf W-Raum \\\
$ (\Omega, \calA, \PP) $, dann wird die \textbf{Verteilungsfunktion} von $ \xi $, die die Verteilung $ \PP_\xi $ eindeutig festlegt, definiert durch
$$
F_\xi: \R^k \ni x \longrightarrow F_xi(x) := \PP(\xi \leq x) \in [0,1]
$$
\textit{Man schreibt auch $ \xi \sim F $, um anzudeuten, dass $ \xi $ die Verteilung besitzt, die zur Verteilungsfunktion $ F $ gehört.}
\end{karte}

\begin{karte}[Stochastik-I]{Eigenschaften\\Verteilungs-\\funktion}[]
ZV $ \xi $, $ F_\xi \equiv F $ V-Funk, $ k = 1 $: $ F $ hat die Eigenschaften
\begin{enumerate}[label=(\roman*)]
\item
$ F $ ist mon wachs
\item
$ lim_{x\to\infty} F(x) = 1 $
\item
$ lim_{x\to-\infty} F(x) = 0 $
\item
$ F $ ist rechtss stet: $ \lim_{n\to\infty} F(x_n) = F(x) $
\item
$ F $ hat linkss Limiten: $ \lim_{n\to\infty} F(x_n) $ existiert
\item[]
\textit{(für iv) und v): $ \forall (x_n)_{n \in \N} \subset \R $ mit $ x_n \downarrow_n x $)}
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Quantil-\\funktion,\\Quantil-\\transformation}[3.1.4]
Sei $ F: \R \ni x \longrightarrow F(x) \in [o,1] $ eine Verteilungsfunktion, dann die \textbf{Quantilfunktion} oder \textbf{Quantiltransformation} zu $ F $ definiert durch:
$$F^{-1}: \ ]0,1] \ni u \longrightarrow F^{-1} := inf\{x: F(x) \geq u\} \in \overline{\R}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Gleichverteilung\\auf dem\\Einheits-\\intervall}[]	
Eine Zufallvariable $ \eta $ heißt \textbf{gleichverteilt} auf dem Einheitsintervall gdw
\cmhalf
$$
F_\eta (x) =
\threepartdef{0}{x \leq 0}{x}{0 < x < 1}{1}{x \geq 1}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Darstellungssatz \\ von \\ Skorokhod}[]	
Sei $ F: \R \ni x \longrightarrow F(x) \in [0,1] $ eine Verteilungsfunktion, $ F^-1 $ die zugehörige Quantilfunktion, $ \eta $ eine auf dem Einheitsintervall gleichverteilte Zufallsvariable, dg: \cmone \newline
$ \xi := F^{-1} (\eta) $ ist eine nach $ F $ verteilte Zufallsvariable
\end{karte}

\begin{karte}[Stochastik-I]{stetig, \\ diskret \\ W-Funktion}[3.1.9]
Die Verteilungsfunktion $ F $ kann stetig oder diskret sein. Dementsprechend kann die zugehörige Zufallsvariable $ \xi $ \textbf{stetig verteilt} oder \textbf{diskret verteilt} sein. Im diskreten Fall bestimmt die \textbf{Wahrscheinlichkeitsfunktion} ebenfalls auf eindeutige Weise die Verteilung von $ \xi $ mit \\
$$
p_\xi \equiv p_F \equiv p: S \ni x \longrightarrow p(x) := F(x) - F(x-) \in \, ]0,1]
$$
\textit{Die Wahrscheinlichkeitsfunktion bestimmt für die s Sprungstellen, ''wie hoch gesprungen wird''.}
\end{karte}

\begin{karte}[Stochastik-I]{Zerlegungssatz \\ für \\ Verteilungsfunktionen}[3.1.10]	
Sei $ F $ eine bel Verteilungsfunktion, dg:
\begin{enumerate}[label=(\roman*), noitemsep]
\item
Es gibt genau zwei Sub-Verteilungsfunktionen $ F_d $ und $ F_c $ mit $ F(x) = F_d (x) + F_c (x) \ \forall x \in \R $. Dabei ist $ F_c $ stetig und $ F_D $ eine Treppenfunktion.
\item
Zur stetigen Sub-Verteilungsfunktion $ F_c $ aus Teil (i) gibt es genau zwei weitere Sub-Verteilungsfunktionen $ F_ac $ und $ F_s $ mit $ F_c(x) = F_ac (x) + F_s (x) \ \forall x \in \R$. Dabei ist das zu $ F_ac $ gehörende Maß abs-stetig und das zu $ F_s $ gehörende Maß singulär bzgl dem Lebesgue-Maß.
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Zusammenhang von \\ Wahrscheinlichkeit, \\ Maßintegral und \\ Verteilung}[3.1.12]
$ \xi $ ZV über $ (\Omega, \calA, \PP) $, abs-stet Vert $ \PP_\xi $ bzgl L-Maß $ \lambda \ \Longrightarrow $  (Rad-Nik) $ \exists $ Borel-mb Funk $ f: \R \ni x \longrightarrow f(x) \in \R_\geq $ mit \\
$$
\PP_\xi(B) = \int_{}^{} I_B (x)f(x)\lambda\t{(d}x\t{)} \ \forall B \in \calB^*
$$
$ \int_{}^{} \ ... \ \lambda \t{(d}x\t{)} $ ist Maßintegral bzgl Lebesque-Maß. Ist $ B = \ ]-\infty,y] $ ein Halbstrahl, $ f $ eine mind stückw stet Funk, dg \\
$$
\int_{}^{} I_{]-\infty,y]} (x) f(x) \lambda \t{(d}x\t{)} = F_\xi (y) = \int_{-\infty}^{y} f(x) \t{(d}x\t{)}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Hauptsatz \\ Differential- \\ und \\ Integralrechnung}[unter 3.1.12]
$$
F'_\xi = f(x) \ \forall x \in \R
$$
\end{karte}

\begin{karte}[Stochastik-I]{Bedingte \\ Wahrscheinlichkeit}[unter 3.2]
W-Raum $ (\Omega, \calA, \PP) $ mit $ A,B \in \calA $, $ \PP(B) > 0 $, dg: \cmone
$$
\PP(A|B) = \frac{\PP(A \cap B)}{\PP(B)}
$$
\end{karte}

\begin{karte}[Stochastik-I]{Bedingte \\ Wahrscheinlichkeit \\ bei \\ Unabhängigkeit }[]	
W-Raum $ (\Omega, \calA, \PP) $ mit $ A,B \in \calA $, $ \PP(B) > 0 $, dg, und zusätzlich $ A $ unabh von $ B $, dg:
$$
\PP(A \cap B) = \PP(A) \cdot \PP(B)
$$
und damit insgesamt
$$
\PP(A) = \frac{\PP(A) \cdot \PP(B)}{\PP(B)} = \frac{\PP(A \cap B)}{\PP(B)} = \PP(A|B) = \PP(A)
$$
\end{karte}

\begin{karte}[Stochastik-I]{stochastische \\ Unabhängigkeit}[3.2.1]	
W-Raum $ (\Omega, \calA, \PP) $, dann sind die Mengensysteme $ (\calC_i)_{i \in I} $, $\calC_i \subset \calA $, $ i \in I $, \textbf{stochastisch unabhängig}, wenn für jede endliche Teilmenge $ \emptyset \neq S \subset I $ gilt:
$$
\PP \left( \bcupp{i \in S}{}{C_i} \right) = \prodd{i \in S}{}{\PP(C_i)} \  \forall C_i \in \calC_i, \ i \in S
$$
\end{karte}

\begin{karte}[Stochastik-I]{unabhängige \\ $\sigma$-Algebren}[3.2.2]
W-Raum $ (\Omega, \calA, \PP) $, $ (\calC_i)_{i \in I} $ unabh Mengensysteme, \\
$ \calC_i \subset \calA $ und $ \cap $-stabil, $ i \in I $, dg: \cmhalf
$$
(\sigma(\calC_i))_{i \in I}
$$ \cmhalf
unabhängig.
\end{karte}

\begin{karte}[Stochastik-I]{Blockungslemma}[3.2.3]	
W-Raum $ (\Omega, \calA, \PP)$, unabh Mengen-Sys $ (\calC_i)_{i \in I} $, $ \calC_i \subset \calA $ und $ \cap $-stabil, $ i \in I $, $ (I_j)_{j \in T} $ pd Teilmengen von $ I $, dg: \cmone
$$
(\calC_{I_j})_{j \in T} \t{, mit } \calC_{I_j} := \sigma \left( \bcupp{i \in I_j}{}{\calC_i} \right) \!, \ j \in T
$$
\end{karte}

\begin{karte}[Stochastik-I]{unabhängige \\ Zufallsvariablen}[3.2.4]	
Eine Familie $ (\xi_i)_{i \in I} $ von ZV über W-Raum $ (\Omega, \calA, \PP) $ heißen \textbf{unabhängig} gdw $ (\sigma(\xi_i))_{i \in I} $ unabh Mengen-Sys auf $ \calA $ sind. \\
Die von $ \xi $ erz $ \sigma $-Algebra auf $ \Omega $ wird bezeichnet mit
$$
\sigma(\xi) = \sigma(\xi^{-1}(\calB^*)) \equiv \xi^{-1}(\calB^*)
$$
\textit{()Urbilder von $ \sigma $-Algebren sind $ \sigma $-Algebren)}
\end{karte}

\begin{karte}[Stochastik-I]{IID}[3.2.5]	
Wiederholt man ein Experiment (ZV!) mehrfach und unabhängig, so sind die $ n  $ ZV $ \xi_1, \xi_2, ... , \xi_n $ stochastisch unabhängig und haben die gleiche Verteilung. Damit sind sie
\center{IID} \\
\center{independen and identically distributed}
\end{karte}

\begin{karte}[Stochastik-I]{Wahrscheinlichkeit \\ mehrdimensionaler \\ Zufallvektor}[3.2.6]
$ (\xi)_{1 \leq i \leq n}, n \in \N $ seien ZV auf $ (\Omega, \calA, \PP) $, dg bzgl des $ n $-dim ZV auf $ (\R^n, \calB_n^*) $ eine Äquivalenz: \\
$ \xi: (\Omega, \calA, \PP) \ni w \longrightarrow \xi(w) := \left( \xi_1(w), ... \, , \xi_n(w) \right) ' \in (\R^n, \calB_n^*) $
\begin{enumerate}[label=(\roman*), noitemsep]
\item
$ (\xi_i)_{1 \leq i \leq n} $ sind unabhängig 
\item
$ \PP_\xi $, die Verteilung von $ \xi $ auf $ (\R^n, \calB_n^*) $ ist identisch mit dem Produktmaß der einz Vert $ \PP_\xi, 1 \leq i \leq n $, dh
$$
\left( \R^n, \calB_n^*, \PP_\xi \right) = \left( \R^n, \botimess{i=1}{n}{\calB*}, \botimess{i=1}{n}{\PP_{\xi_i}} \right)
$$
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Faltungsformel \\ diskret}[3.2.7-i]
Zwei unabh ZV $ \xi, \eta $ über $ (\Omega, \calA, \PP) $, $ \varphi = \xi + \eta $: \cmhalf \\
Falls die ZV diskret mit mögl Sprungstellen \\
$ S_\xi = (a_i)_{i \in \N} \subset \R $ bzw $ S_\eta = (b_i)_{i \in \N} \subset \R $ dann ist auch $ \varphi $ diskret und es gilt $ \forall x \in \R $:
$$
\PP(\varphi \leq x) = \summ{i \geq 1}{}{ \PP_\xi({a_i})F_\eta(x-a_i) } = \summ{i \geq 1}{}{ \PP_\eta({b_i})F_\xi(x-b_i) }
$$
\end{karte}

\begin{karte}[Stochastik-I]{Faltungsformel \\ stetig}[3.2.7-ii]
Zwei unabh ZV $ \xi, \eta $ über $ (\Omega, \calA, \PP) $, $ \varphi = \xi + \eta $: \cmhalf \\
Falls die ZV abs-stet bzgl des Lebesgue-Maßes mit Dichten \\
$ f_\xi, f\eta $ dann gilt $ \forall x \in \R $:
\begin{align*}
\PP(\varphi \leq x) = \int_{-\infty}^{\infty} f_\xi (y) F\eta (x-y) \lambda(\t{d}y) \\ 
= \int_{-\infty}^{\infty} f_\eta (z) F_\xi (x-z) \lambda(\t{d}z)
\end{align*}
\end{karte}

\begin{karte}[Stochastik-I]{Faltung \\ diskreter \\ Zufallsvariablen \\ diskret}[3.2.8-i]
Zwei unabh ZV $ \xi, \eta $ über $ (\Omega, \calA, \PP) $, $ \varphi = \xi + \eta $: \cmhalf \\
Falls die ZV diskret mit mögl Sprungst $ S_\xi = (a_i)_{i \in \N} \subset \R $ \\
bzw $ S_\eta = (b_i)_{i \in \N} \subset \R $ dann ist auch $ \varphi $ diskret mit mögl Sprungstellen $ c_k = a_i + b_j $ und es gilt $ \forall x \in \R $:
\begin{align*}
\PP(\varphi \leq x)
= \summ{i \geq 1}{}{ \PP_\xi(\{a_i\}) \PP_\eta(\{c_k-a_i\})} \\
= \summ{i \geq 1}{}{ \PP_\eta(\{b_i\}) \PP_\xi(\{c_k-b_j\})}
\end{align*}
\end{karte}

\begin{karte}[Stochastik-I]{Faltung \\ absolut-stetiger \\ Zufallsvariablen \\ absolut-stetig}[3.2.8-ii]
Zwei unabh ZV $ \xi, \eta $ über $ (\Omega, \calA, \PP) $, $ \varphi = \xi + \eta $: \cmhalf \\
Falls die ZV abs-stet bzgl des Lebesgue-Maßes mit Dichten \\
$ f_\xi, f\eta $ dann ist auch $ \varphi $ abs-stet bzgl des Lebesgue-Maßes mit Dichte
$$
f_\varphi: \R \ni x \longrightarrow f_\varphi (x) := \int_{-\infty}^{\infty} f_\xi (y) f_\eta (x-y) \lambda (\t{d}y) \in \R_\geq
$$
\end{karte}

\begin{karte}[Stochastik-I]{Bernoulli- \\ Verteilung}[3.3.1]
\vspace*{1 cm}
Eine ZV besitzt eine \textbf{Bernoulli-Verteilung} zum \newline
Erfolgsparameter $0\leq p \leq 1$, kurz $\xi \sim BERN(p)$, \newline
wenn $S = \{0,1\}$ und $\PP_\xi(\{1\}) = p$
\vspace*{0.3 cm} \\
\end{karte}

\begin{karte}[Stochastik-I]{Binomial- \\ Verteilung}[3.3.1]
Eine ZV $\xi$ besitzt eine \textbf{Binomial-Verteilung} zu \newline
den Parametern $n,p$ mit $n \in \N$ und $0 < p < 1$, \newline
kurz $\xi \sim BINOM(n,p)$, wenn $S = {0,1,...,n}$, $\xi = \sum_{i=1}^{n} \eta_i$: \cmhalf \\
$$
\PP_\xi(\{k\}) = \binom{n}{k}p^k(1 - p)^{n-k} \quad \forall k \in S
$$ \cmhalf \\
\textit{Die binomialverteilte ZV entsteht also als Summe IID. Bernoulli Variablen}
\end{karte}

\begin{karte}[Stochastik-I]{Grenzwertsatz \\ von \\ Poisson}[3.3.3]
Sei $(\xi_n)_{n \in \N}$ eine Folge von binomial-verteilten ZV über $(\Omega, \calA, \PP)$ mit $\xi_n \sim BINOM(n,p_n)$ für $n \in \N$ und $n \, p_n = \lambda > 0 \ \forall n \in \N$, dg:
\vspace*{0.3 cm} \\
$$
\lim_{n \to \infty} \PP(\xi_n = k) = e^{-\lambda} \left( \frac{\lambda^k}{k!} \right) \quad \forall k \geq 0
$$
\vspace*{0.3 cm} \\
\textit{(Beispielsweise radioaktiver Zerfall; extrem große Menge an Experimenten benötigt.)}
\end{karte}

\begin{karte}[Stochastik-I]{Poisson- \\ Verteilung}[3.3.4]
Eine ZV $\xi$ besitzt eine \textbf{Poisson-Verteilung } zum \newline
Parameter $\lambda > 0$, kurz $\xi \sim POIS(\lambda)$, wenn $S = \N_0$ und \mmthree \newline
$$
\PP_\xi (\{k\}) = \frac{\lambda^k}{k!} \, e^{(-\lambda)}
$$ \mmthree \newline
\textit{(Große Population/seltenes Merkmal: zugrunde liegende Binomial-Vert durch Poisson-Vert ersetzen mit $\lambda = np$, n = Populationsgröße, $p$ = W., dass Individuum Merkmal trägt; z.B. Fälle seltener Krankheit in großer Stadt/Jahr.)}
\end{karte}

\begin{karte}[Stochastik-I]{Multinomial- \\ Koeffizient}[3.3.3]
Für $k_1, ... , k_m$ mit $\sum_{i=1}^{m} k_i = n$ definiert durch
$$
\binom{n}{k_1,k_2,...,k_m} := \frac{n!}{k_1! \cdot k_2! \cdot \cdot \cdot k_m!}
$$
$$
\varphi := \left( \summ{i=1}{n}{I(\eta_i = 1)}, \, \summ{i=1}{n}{I(\eta_i = 1)}, ... \, , \, \summ{i=1}{n}{I(\eta_i = 1)} \right) ^T
$$
$$
\PP(\varphi = (k_1,k_2, ... \, k_m)^T) = \binom{n}{k_1,k_2, ... \, k_m} p_1^{k_1} \cdot p_2^{k_2} \cdot \cdot \cdot p_m^{k_m} 
$$
\end{karte}

\begin{karte}[Stochastik-I]{Multinomial- \\ Verteilung}[3.3.6]
Bei einer \textbf{Multinomial-Verteilung} $\varphi$ zu Parametern $(n,p_1,p_2, ... \, p_m)$, kurz $\varphi \sim MULTINOM(n,p_1,p_2, ... \, p_m)$ kann der zugehörige diskrete ZV nur folgende Werte annehmen
$$
S = \{ (k_1,k_2, ... \, k_m)^T: \summ{i=1}{m}{k_i = n}, \, k_i \in \N_0, \, 1 \leq i \leq m \}
$$
\textit{(Dies ist eine Verallgemeinerung der Binomial-Verteilung, bei der die $n$ Objekte nicht auf zwei, sonderm auf $m$ Gruppen verteilt werden.)}
\end{karte}

\begin{karte}[Stochastik-I]{Geometrische \\ Verteilung}[3.3.7]
ZV $\xi$ hat \textbf{geometrische Verteilung} zum Parameter $ 0 < p < 1$, kurz $\xi \sim GEOM(p)$, falls $S = N_0$ und $ \forall k \in S$:
$$
\PP_\xi(\{k\}) = (1 - p)^k p
$$
\textit{(z.B. Wartezeit eines (nicht lernenden) Schützen bis zum ersten Treffer ($\xi = k$: der Schütze trifft beim Schuss $k + 1$)}
\end{karte}

\begin{karte}[Stochastik-I]{Hypergeometrische \\ Verteilung}[3.3.8]
ZV $\xi$ hat \textbf{Hypergeometrische Verteilung} zum  \newline
Parameter $(n,r,s)$ mit $n,r,s \in \N$ un $n \leq r$, \newline
kurz $\xi \sim HYPER(n,r,s)$, falls $S = {0,1, ... \, n}$ und $\forall k \in S$:
$$
\PP_\xi(\{k\}) = \frac{\binom{r}{k} \binom{s}{n-k}}{\binom{r+s}{n}}
$$
\textit{(Urne mit $r$ roten und $s$ schwarzen Kugeln, dann gibt die ZV $\xi \sim HYPER(n,r,s)$ die Anzahl gezogener roter \newline
Kugeln beim $n$-maligen Ziehen ohne Zurücklegen an.)}
\end{karte}

\begin{karte}[Stochastik-I]{Jacobi- \\ Matrix}[3.3.9]
$T: \R^n \ni x \longrightarrow T(x) \in \R^n$ diff-bare Abb mit stet Abl gdw $\forall T_i: T_i$ ist partiell stet diff-bar mit \newline
$D_j T_i(x) = \frac{\partial T_i (x)}{\partial x_j}, \, 1 \leq i \leq n$,
dann ist $T'(x)$ die \textbf{Jacobi-Matrix} von $T$ an der Stelle $x$, dh
\mmthree
$$
T'(x) := 
\left( \begin{matrix}
D_1 T_1 (x) & D_2 T_1 (x) & \dots & D_1 T_n (x) \\
D_1 T_2 (x) & D_2 T_2 (x) & \dots & D_2 T_n (x) \\
\vdots      & \vdots      & \vdots & \vdots      \\
D_1 T_n (x) & D_2 T_n (x) & \dots & D_n T_n (x) \\
\end{matrix} \right )
$$
\end{karte}

\begin{karte}[Stochastik-I]{Transformations- \\ Formel}[3.3.10]
Sei $T: (\R^n, \calB_n^*, \lambda^n) \ni x \longrightarrow T(x) \in (\R^n, \calB_n^*)$ Transf:
\begin{enumerate}[label=(\roman*), noitemsep]
\item $T(x)$ hat stet part Abl an jeder Stelle $x \in \R^n$
\item $T$ ist bij, $T^{-1}$ hat stet part Abl an jeder Stelle $y \in \R^n$
\end{enumerate}
Dann hat das Bildmaß von $\lambda^n$ unter $T$, dh \mmthree \newline
$\lambda_T^n \equiv (\lambda^n)_T: \calB_n^* \ni B \longrightarrow \lambda_t^n(B): \lambda^n(T^{-1}(B)) \in \R_\geq$  \mmthree \newline
eine Radon-Nikodym Dichte $f$ bzgl $\lambda^n$, dh d$\lambda_T^n = f$ d$\lambda^n$:  \mmthree \newline
$f(y) = | \, det((T^{-1}) ' (y))|$
\end{karte}

\begin{karte}[Stochastik-I]{Transformations- \\ Formel \\ für \\ Unterräume ???!!!}[3.3.11]
Transf-Formel gilt auch für Abb vom Typ
$$
T : (G, G \cap \calB_n^*, \lambda^n) \ni x \longrightarrow T(x) \in (T(G), T(G) \cap \calB_n^*)
$$
wenn $T$ gemäß Transf-Formel und $G \subset \R^n$ ist off Menge, \newline
und dann ist die \textbf{Spur-}$\bm{\sigma}$\textbf{-Algebra} der Borel-Mengen des $\R^n$ auf $G$: \mmthree \newline
$$
G \cap \calB_n^* := \{ G \cap B : B \in \calB_n^*\}
$$
\end{karte}

\begin{karte}[Stochastik-I]{neuer \\ Zufallsvektor \\ mit eigener \\ Lebesque-Dichte}[3.3.12]
Sei $\xi$ ein ZV in $(\R^n, \calB_n^*)$ über $(\Omega,\calA,\PP)$ mit Vert $\PP_\xi$ die abs-stet- bzgl des Lebesque-Maßes $\mu^n$ ist, die Dichte sei gegeben durch $f_\xi \equiv f : \R^n \ni x \longrightarrow f(x) \in \R_\geq$, $T : \R^n \longrightarrow \R^n$ sei eine Abb nach der Transformationsformel, dann wird wieder ein ZV definiert durch \mmthree \newline
$\eta : (\Omega,\calA,\PP) \ni w \longrightarrow \eta(w) := T(\xi(w)) \in (\R^n,\calB_n^*)$ \mmthree \newline
mit der Lebesque-Dichte \mmthree \newline
$g_\eta \equiv g :  \R^n \ni y \rightarrow g(y) := f(T^{-1}(y))| det((T^{-1}) (y)) | \in \R_\geq$

\end{karte}

\begin{karte}[Stochastik-I]{Erwartungswert \\ Vektor und \\ Matrix}[unter 3.6.1]
\begin{spreadlines}{10 pt}
Zufallsvektor, Vert-Fkt und Erw-Wert Zufallsvektor \newline
komp-weise sowie Ert-Wert Matrix:
\begin{gather*}
\xi: (\Omega,\calA,\PP) \ni \omega \rightarrow \xi(\omega) := \left( \xi_1 (\omega), ... \, , \xi_n (\omega) \right)' \in (\R^n,\calB_n*) \\
F_\xi \equiv F : R^n \ni x \longrightarrow F(x) := \PP(\xi \leq x) \in [0,1] \\
\E(\xi) := \left( \E(\xi_1, ... \, , \E(\xi_n) \right)' \\
\E(\xi) := \left( \E(\xi_{i,j}) \right)_{1 \leq i \leq n,1 \leq j \leq m} 
\end{gather*}
\end{spreadlines}
\end{karte}


\begin{karte}[Stochastik-I]{Kovarianz \\ und \\ Varianz-Kovarianz}[3.6.1]
Seien $\xi = (\xi_1, ... \, , \xi_n)',$ und $\eta = (\eta_1, ... \, , \eta_m)'$ ZV mit Erw-Werten $\mu = (\mu_1, ... \, , \mu_n)$ und $\nu = (\nu_1, ... \, , \nu_n)$, und $\forall 1 \leq i\leq n, \, \forall 1 \leq m \leq m: \E(|\xi_i\eta_j|) < \infty$, dg (Kov-Matrix):
\setlength\abovedisplayskip{0 pt}
\begin{spreadlines}{5 pt}
\begin{gather*}
Cov(\xi,\eta) = \bm{C} := (c_{i,j})_{1 \leq i \leq n, 1 \leq j \leq m} \\
\left( \begin{matrix}
\E((\xi_1 - \mu_1)(\eta_1 - \nu_1)) & \dots &  \E((\xi_1 - \mu_1)(\eta_m - \nu_m))\\
\vdots      & \vdots      & \vdots      \\
\E((\xi_n - \mu_n)(\eta_1 - \nu_1)) & \dots &  \E((\xi_n - \mu_n)(\eta_m - \nu_m))\\
\end{matrix} \right ) \\
\t{mit } c_{i,j} := \E((\xi_i - \mu_i)(\eta_j - \nu_j)) = \E(\xi_i, \eta_j) - \mu_i\nu_j
\end{gather*}
\end{spreadlines}
\end{karte}

\begin{karte}[Stochastik-I]{Rechenregeln \\ Erwartungswert \\ und \\ Varianz-Kovarianz}[3.6.2]
Seien \textbf{A, B, C} Matrizen mit Dim $l \times n, m \times p, l \times p$, \newline
$\xi = (\xi_i)_{1 \leq i \leq n}, \, \eta = (\eta_i)_{1 \leq i \leq p} $ Zufallsvektoren und \newline
$ \bm{\vartheta} = (\vartheta_{i,j})_{1 \leq i \leq n, \, 1 \leq j \leq m}$ eine zufällige Matrix, dg:
\begin{enumerate}[label=(\roman*)]
\item $ \E(\bm{A \cdot \vartheta \cdot B + C}) = \bm{A} \cdot \E(\bm{\vartheta}) \cdot \bm{B} + \bm{C} $
\item $ Cov(\bm{A} \cdot \xi, \, \bm{B} \cdot \eta) = \bm{A} \cdot Cov(\xi,\eta) \cdot \bm{B}' $
\end{enumerate}
\end{karte}

\begin{karte}[Stochastik-I]{Diagonalisierbarkeit \\ Symmetrie \\Eigenwerte \\ Wurzel}[3.6.3]
Wenn \textbf{C} exist für einen ZV, dann ist sie symm, dann ist \tb{C} n-neg definit und diag-bar mit reellen, n-neg Eigen-W zur Diag-Matr \tb{D} mit der orthog. Matrix \tb{Q},und es gelten:
\begin{gather*}
\bm{Q' \cdot C \cdot Q = D} \\
\left( \bm{Q \cdot D^{\frac{1}{2}} \cdot Q^{-1}} \right) \left( \bm{Q \cdot D^{\frac{1}{2}} \cdot Q^{-1}} \right) = \left( \bm{Q \cdot D \cdot Q^{-1}} \right) = \bm{C} \\
\bm{C^{\frac{1}{2}}} := \bm{Q \cdot D^{\frac{1}{2}} \cdot Q^{-1}} \\
\end{gather*}
\textit{(Die Wurzel aus \tb{D} ist komponentenweise definiert)}
\end{karte}

\begin{karte}[Stochastik-I]{erweiterte Definition \\ Charakteristische \\ Funktion \\ für Zufallsvektoren}[3.6.4]
Sei $\xi$ ein $n$-dim ZV über $(\Omega,\calA,\PP)$, dann ist die \tb{Charakteristische Funktion} dazu (mit dem üblichen Skalarprodukt $t' \cdot x im \R^n$):
$$
C_\xi \equiv C : \R^n \ni t \rightarrow C(t) := \E(e^{it' \cdot \xi}) = \int_{}^{} e^{it' \cdot x} \PP_\xi (\t{d} x) \in \C
$$
Bemerkung: Die Verteilungen $\PP_\xi$ und $\PP_\eta$ zweier ZV auf $(\R^n, \calB_n^*)$ sind identisch gdw ihre Charakteristischen Funktionen übereinstimmen!
\end{karte}

\end{document}